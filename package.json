{
  "name": "monorepo-playground",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "repo:app": "pnpm --filter @kurly-frontend/app",
    "preinstall": "pnpm dlx only-allow pnpm"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=18.12.1",
    "pnpm": ">=7.16.0"
  },
  "packageManager": "pnpm@7.16.0",
  "devDependencies": {
    "pnpm": "^7.16.0",
    "turbo": "^1.6.3"
  }
}
